events { worker_connections 1024;}
http {
    include mime.types;
    default_type application/octet-stream;

    upstream backend_app {
        server app_prod:3000;
    }
    include /etc/nginx/sites-enabled/*.conf;
}